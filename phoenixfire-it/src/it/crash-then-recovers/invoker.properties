# The test hard-crashes the JVM on its first attempt, then passes on the escalated retry.
# Because it ultimately recovers (and failOnFlakyTests is false), the build must SUCCEED.
invoker.goals = test
invoker.buildResult = success
