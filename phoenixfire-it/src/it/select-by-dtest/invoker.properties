# -Dtest=Checks#fast* selects only the two fast methods of the non-default-named Checks class.
# SlowTest (default-named, would fail) must be excluded, so the build succeeds.
invoker.goals = test
invoker.buildResult = success
