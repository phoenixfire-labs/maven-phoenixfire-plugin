# Fails an assertion once in the shared pool, then recovers in an isolated fork; build succeeds
# (recovered/flaky tests do not fail the build by default).
invoker.goals = test
invoker.buildResult = success
