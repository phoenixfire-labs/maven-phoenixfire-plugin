# The crashing test can never pass, so the Phoenixfire build is expected to fail.
# The verify.groovy script asserts that a complete report was still produced.
invoker.goals = test
invoker.buildResult = failure
