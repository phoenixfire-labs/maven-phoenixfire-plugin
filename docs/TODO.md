# Phoenixfire TODO

Planned work now lives as **one markdown plan per topic** under [`docs/todos/`](todos/README.md).

| Plan | Summary |
|------|---------|
| [semantic-fork-ids.md](todos/semantic-fork-ids.md) | Semantic `fork` labels in reports (`fork-1`/`fork-2`, `fork-isolated`, …) |
| [junit-platform-launcher-classpath.md](todos/junit-platform-launcher-classpath.md) | Auto-resolve matching `junit-platform-launcher` on fork classpath |

Add new plans in `docs/todos/` and link them from [todos/README.md](todos/README.md).
